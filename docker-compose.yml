services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: todd
      POSTGRES_USER: todd
      POSTGRES_PASSWORD: todd
    ports:
      - "5432:5432"
    volumes:
      - todd_postgres:/var/lib/postgresql/data

volumes:
  todd_postgres:
